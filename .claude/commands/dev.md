Start the Belva-GEN development server.

Usage: /dev

Run `npm run dev` to start the Next.js development server on http://localhost:3000.

The dashboard is available at http://localhost:3000/dashboard.
